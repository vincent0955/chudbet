# Copy to `.env.local` to override. Used at build time (Vite embeds `VITE_*`).
VITE_API_URL=http://localhost:8000
# Backend demo wallet (`GET /accounts/{id}`). Default 1 with docker-compose seed env.
VITE_ACCOUNT_ID=1
