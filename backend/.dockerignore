__pycache__
*.py[cod]
.pytest_cache
.venv
venv
.env
*.md

# Omit ingestion package from the API image: Docker BuildKit on Windows (often under OneDrive)
# can fail with "invalid file request" when transferring this folder. Ingestion does not run
# inside uvicorn; run the CLI on the host instead (see README).
app/ingestion/
